{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Open Structurizr Preview",
      "type": "process",
      "command": "${execPath}",
      "args": [
        "--reuse-window",
        "--command",
        "structurizr.preview",
        "${workspaceFolder}/stepwize.dsl"
      ],
      "problemMatcher": []
    }
  ]
}
